<h1><%= pageTitle %></h1>
<p>
    <% if(messages) {%>
    <h2>Votre demande n'a pas abouti.</h2>
    <ul>
        <% messages.forEach( erreur => {%>
            <li style="color: red"><%=erreur.msg%></li>
        <%})%>
    </ul>

    <%} else {%>

    <h2>Merci pour votre message.</h2>
    Cher <%=contact.name%>, vous avons reçu votre message. (<%=contact.email%>)
    <%}%>
</p>